# Import limitation

This GitHub import preserves the audited application source from ChatGPT Sites revision `a34e14f`. The generated `package-lock.json` is intentionally not mirrored here because it exceeded the connector’s safe single-file transfer limit. The exact original revision remains preserved in the owner’s private archive. Run `npm install` to regenerate the lockfile when needed.
